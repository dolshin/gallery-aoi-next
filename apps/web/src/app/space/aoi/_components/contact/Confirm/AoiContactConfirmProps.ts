import type { RequestCookie } from 'next/dist/compiled/@edge-runtime/cookies';

/**
 * Gallery Aoi お問い合わせ確認フォームコンポーネントのプロップス
 */
export type AoiContactConfirmProps = {
  cookieValue: RequestCookie | null;
};
