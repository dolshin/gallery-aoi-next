import { style } from '@vanilla-extract/css';

/**
 * Styles
 */
export const footer = style({
  marginTop: '150px',
  marginBottom: '30px',
  width: '100%',
});

export const copyRight = style({
  fontSize: '11px',
  padding: '0em 3em',
  letterSpacing: '0.1em',
});
