import { style } from '@vanilla-extract/css';

export const wrapper = style({
  margin: 0,
  overflow: 'hidden',
  borderRadius: '16px',
});

export const image = style({
  width: '100%',
  height: 'auto',
  borderRadius: '16px',
});
